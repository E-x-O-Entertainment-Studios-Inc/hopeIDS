version: '3.8'

services:
  hopeid:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: hopeid-test
    ports:
      - "3333:3333"
    environment:
      - PORT=3333
      - NODE_ENV=production
    restart: unless-stopped
